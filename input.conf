# Lines starting with # are comments. Use SHARP to assign the # key.
# List of special keys: --input-keylist
# Keybindings testing mode: mpv --input-test --force-window --idle
#
# Use 'ignore' to unbind a key fully (e.g. 'ctrl+a ignore').
#
# Strings need to be quoted and escaped:
# KEY show-text "This is a single backslash: \\ and a quote: \" !"
#
# You can use modifier-key combinations like Shift+Left or Ctrl+Alt+x with
# the modifiers Shift, Ctrl, Alt and Meta (may not work on the terminal).
#
# The default keybindings are hardcoded into the mpv binary.
# You can disable them completely with: --no-input-default-bindings
#
# If this is enabled, treat all the following bindings as default.
default-bindings start

# Mouse
MBTN_LEFT     ignore              # don't do anything
MBTN_LEFT_DBL cycle fullscreen    # toggle fullscreen on/off
MBTN_RIGHT    cycle pause         # toggle pause on/off
WHEEL_UP      seek 15
WHEEL_DOWN    seek -15
WHEEL_LEFT    ignore
WHEEL_RIGHT   ignore

# Arrow keys
# Seek units are in seconds, but note that these are limited by keyframes
RIGHT seek  5
LEFT  seek -5
Shift+RIGHT no-osd seek  88 exact
Shift+LEFT  no-osd seek -88 exact
Shift+UP    no-osd seek  5 exact
Shift+DOWN  no-osd seek -5 exact
# Skip to previous/next subtitle (subject to some restrictions; see manpage)
Ctrl+LEFT   no-osd sub-seek -1
Ctrl+RIGHT  no-osd sub-seek  1
# Adjust timing to previous/next subtitle
Ctrl+Shift+LEFT sub-step -1
Ctrl+Shift+RIGHT sub-step 1
# Move video rectangle
#Alt+left  add video-pan-x  0.1
#Alt+right add video-pan-x -0.1
#Alt+up    add video-pan-y  0.1
#Alt+down  add video-pan-y -0.1

# Numpad
# Zoom/unzoom video
Alt++     add video-zoom   0.1
Alt+-     add video-zoom  -0.1
# Reset video zoom/pan settings
Alt+BS set video-zoom 0 ; set video-pan-x 0 ; set video-pan-y 0
PGUP add chapter 1                     # skip to next chapter
MBTN_FORWARD add chapter 1
PGDWN add chapter -1                   # skip to previous chapter
MBTN_BACK add chapter -1
Shift+PGUP ignore
Shift+PGDWN ignore
[ multiply speed 1/1.1                 # scale playback speed
] multiply speed 1.1
{ multiply speed 0.5
} multiply speed 2.0
BS set speed 1.0                       # reset speed to normal
Shift+BS revert-seek                   # undo previous (or marked) seek
Shift+Ctrl+BS ignore
q quit
Q quit-watch-later
q {encode} quit 4
ESC set fullscreen no
ESC {encode} quit 4
. frame-step                           # advance one frame and pause
, frame-back-step                      # go back by one frame and pause
SPACE cycle pause
> playlist-next                        # skip to next file
< playlist-prev                        # skip to previous file
O no-osd cycle-values osd-level 3 1    # cycle through OSD mode
o show-progress
i script-binding stats/display-stats
I script-binding stats/display-stats-toggle
z add sub-delay -0.1                   # subtract 100 ms delay from subs
Z add sub-delay +0.1                   # add
x ignore
ctrl++ ignore
ctrl+- ignore
9 add volume -2
/ ignore
0 add volume 2
* ignore
m cycle mute
#1 add contrast -1
#2 add contrast 1
#3 add brightness -1
#4 add brightness 1
#5 add gamma -1
#6 add gamma 1
#7 add saturation -1
#8 add saturation 1
Alt+0 ignore
Alt+1 ignore
Alt+2 ignore
# toggle deinterlacer (automatically inserts or removes required filter)
d ignore
r add sub-pos -1                       # move subtitles up
R add sub-pos +1                       #                down
t ignore
v ignore
# switch between applying no style overrides to SSA/ASS subtitles, and
# overriding them almost completely with the normal subtitle style
#u cycle-values sub-ass-override "force" "no"
#j cycle sub                            # cycle through subtitles
#J cycle sub down                       # ...backwards
SHARP cycle audio                      # switch audio streams
_ cycle video
T cycle ontop                          # toggle video window ontop of other windows
s async screenshot                     # take a screenshot
S async screenshot video               # ...without subtitles
Ctrl+s async screenshot window         # ...with subtitles and OSD, and scaled
#Alt+s screenshot each-frame            # automatically screenshot every frame
p add panscan -0.1
P add panscan +0.1

A ignore

E cycle edition                        # next edition
l ab-loop                              # Set/clear A-B loop points
L cycle-values loop-file "inf" "no"    # toggle infinite looping
ctrl+c ignore
#DEL script-binding osc/visibility      # cycle OSC display
ctrl+h ignore
F8 show_text ${playlist}               # show playlist
F9 show_text ${track-list}             # show list of audio/sub streams

c script-message-to crop start-crop							 # crop.lua
C script-message-to crop toggle_crop

v-f vf del -1                                                  # crop.lua
t script-message-to seek_to toggle-seeker                    # seek-to.lua


# TOGGLE/CYCLE STUFF
z ignore
z-d cycle deband
z-D cycle deinterlace
z-i cycle interpolation
z-s cycle sub-visibility
z-a cycle mute
z-v cycle sub-ass-vsfilter-aspect-compat
z-T cycle ontop
z-r cycle-values video-aspect "16:9" "4:3" "2.35:1" "-1"
z-t cycle-values tscale "box" "oversample" "linear" "catmull_rom" "mitchell" "gaussian" "bicubic"
z-o cycle-values ass-style-override "yes" "force" "no"
z-w cycle force-window
z-f cycle osd-fractions
z-b cycle-values blend-subtitles "yes" "video" "no"

! apply-profile SSR1; show-text "SSR for 2-pass"
@ apply-profile FSRCNNX1; show-text "FSRCNNX for non 2pass 1080p monitor"
SHARP apply-profile FSRCNNX2; show-text "FSRCNNX for larger than 1080p video"
$ show-text "${height}"
% apply-profile motionblur; show-text "motionblur"

# GLSL Shaders
g ignore
g-f change-list glsl-shaders toggle "~~/shaders/kyoani_fog.glsl"
g-g change-list glsl-shaders clr ""
